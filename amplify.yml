version: 1
applications:
  - appRoot: /
    backend:
      phases:
        build:
          commands:
            - '# Install dependencies'
            - npm install
    frontend:
      phases:
        build:
          commands:
            - '# Build the project'
            - npm run build
      artifacts:
        baseDirectory: /build
        files:
          - '**/*'
